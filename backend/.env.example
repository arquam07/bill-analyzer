DATABASE_URL=postgresql+asyncpg://bill:bill@localhost:5433/bill_analyzer
LOG_LEVEL=INFO
APP_ENV=dev
# Vertex AI (Gemini Flash) — auth via Application Default Credentials
# Run: gcloud auth application-default login  (local dev)
# Cloud Run uses the service account automatically (no key needed)
VERTEX_PROJECT=bill-analyzer07
VERTEX_LOCATION=asia-northeast1
VERTEX_MODEL=gemini-2.0-flash-001
VERTEX_TIMEOUT_SECONDS=60
